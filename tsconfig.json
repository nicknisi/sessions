{
  // site/ is its own Astro project with its own tsconfig and its own
  // `astro check`. It targets the browser and the Astro build, not Bun, so it
  // must not inherit these options.
  // tools/ is vendored lint tooling with its own compiler options; this
  // repo's strict flags (e.g. noUncheckedIndexedAccess) do not apply to it.
  "exclude": ["site", "dist", "node_modules", "tools"],
  "compilerOptions": {
    // Environment setup & latest features
    "lib": ["ESNext"],
    "target": "ESNext",
    "module": "Preserve",
    "moduleDetection": "force",
    "jsx": "react-jsx",
    "allowJs": true,
    "types": ["bun"],

    // Bundler mode
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "noEmit": true,

    // Best practices
    "strict": true,
    "skipLibCheck": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,

    // Some stricter flags (disabled by default)
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noPropertyAccessFromIndexSignature": false
  }
}
